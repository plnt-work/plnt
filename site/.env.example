# Copy to .env (gitignored) for local dev. Do NOT commit real values.

# The plnt server the /playground page talks to (plnt serve --playground).
# For production builds on Vercel, set this in the project's environment variables.
PUBLIC_PLNT_PLAYGROUND_URL=https://plnt-playground.onrender.com
